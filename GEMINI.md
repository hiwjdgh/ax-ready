# Gemini Agent 지침

이 저장소의 공통 Agent 지침은 [AGENTS.md](AGENTS.md)를 따른다.

Gemini 계열 Agent로 작업할 때도 다음을 지킨다.

- AX Ready는 여러 Agent가 사용할 수 있는 스킬 프로젝트다.
- 대상 사용자 프로젝트에 `memory/`를 기본 산출물로 만들지 않는다.
- `memory/`는 이 저장소 자체의 지속 운영 기록이다.
- 공통 산출물은 `AGENTS.md`를 중심으로 하고, Agent별 파일은 짧은 adapter로 유지한다.
